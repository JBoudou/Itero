import { AppMenuTriggerDirective } from './app-menu-trigger.directive';

describe('AppMenuTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMenuTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
